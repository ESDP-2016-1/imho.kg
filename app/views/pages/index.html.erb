<h1>Pages#index</h1>
<p>Find me in app/views/pages/index.html.erb</p>

<% @users.each do |user| %>
  <%= user.name %>
  <%= user.dob %>
  <%= user.email %>
    <%= calculate_age(user.dob) %>
    <%= user.gender.name %>
    <%= user.city.name %>
  <%= image_tag user.avatar.url(:thumb) %>
<% end %>
