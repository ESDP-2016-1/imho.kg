<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <p class="navbar-brand" title="imho.kg">
        <%= link_to image_tag('logo.png', style: "max-width:100px; margin-top: -7px;"), root_path %>
      </p>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <li><a href="#">About</a></li>
        <li><a href="#">Contacts</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <p class="navbar-text">
              Привет, <%= link_to current_user.name, edit_user_path(current_user), class: 'navbar-link' %>
            </p>

            <ul class="nav navbar-nav">
              <li>
                <div class="menu_avatar_block"><%= link_to image_tag(current_user.avatar.url(:thumb), class: "menu_user_avatar"), user_path(current_user)%>
                </div>
              </li>
              
              <li class="dropdown">
                <div class="menu_avatar_dropdown_block">
                  <a class="dropdow-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-triangle-bottom"></span></a> 
                  <ul class="dropdown-menu">
                    <li><%= link_to 'Редактирование профиля', edit_user_registration_path(current_user) %></li>
                    <li class="divider"></li>
                    <li><%= link_to 'Выйти', destroy_user_session_path, method: :delete %></li>
                  </ul>
                </div>
              </li>
            </ul>  
            
        <% else %>
          <%= link_to 'Войти', "#sign_in", "data-toggle" => "modal", 'data-target' => '#sign_in_modal', class: 'btn btn-primary navbar-btn' %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
